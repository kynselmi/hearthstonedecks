<p id="notice"><%= notice %></p>

<p>
  <strong>Card:</strong>
  <%= @in_deck.card_id %>
</p>

<p>
  <strong>Deck:</strong>
  <%= @in_deck.deck_id %>
</p>

<%= link_to 'Edit', edit_in_deck_path(@in_deck) %> |
<%= link_to 'Back', in_decks_path %>
