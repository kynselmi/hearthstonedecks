<br>

<p>
  <strong>Nimi:</strong>
  <%= @deck.name %>
</p>

<p>
  <strong>Käyttäjä:</strong>
  <%= link_to @deck.user.username, @deck.user %>
</p>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Nimi</th>
      <th>Mana</th>
      <th>Attack</th>
      <th>Health</th>
      <th>Teksti</th>
    </tr>
  </thead>

<h3>Pakan kortit</h3>
<tbody>
   <% @deck.cards.each do |card| %>
	    <tr>
	      <td> <%= card.name %></td>
	      <td> <%= card.mana %></td>
	      <td> <%= card.attack %></td>
		    <td> <%= card.health %></td>
	      <td> <%= card.text %></td>
	    </tr>
   <% end %>
  </tbody>
</table>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Nimi</th>
      <th>Mana</th>
      <th>Attack</th>
      <th>Health</th>
      <th>Teksti</th>
      <th>Lisää kortti</th>
    </tr>
  </thead>

<h3>Kaikki kortit</h3>
    <tbody>
      <% Card.all.each do |card| %>
      <tr>
        <td> <%= card.name %></td>
        <td> <%= card.mana %></td>
        <td> <%= card.attack %></td>
        <td> <%= card.health %></td>
        <td> <%= card.text %></td>
        <td> <%= link_to 'Lisää', new_in_deck_path%> </td>
      </tr>
    <% end %>
  </tbody>
</table>


